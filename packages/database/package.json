{
  "name": "@ferramenta/database",
  "version": "1.0.0",
  "private": true,
  "description": "Prisma schema, migrations e seeds do ERP",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./prisma-client": "./src/client.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:prod": "prisma migrate deploy",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "build": "tsc",
    "lint": "eslint src/",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.0.0",
    "prisma": "^6.0.0",
    "typescript": "^5.6.0",
    "tsx": "^4.0.0",
    "rimraf": "^6.0.0"
  }
}